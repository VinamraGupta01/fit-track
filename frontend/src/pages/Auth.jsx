import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import Logo from '../components/Logo'; import { useAuth } from '../context/AuthContext';
export function Login(){return <AuthForm mode="login"/>} export function Register(){return <AuthForm mode="register"/>}
function AuthForm({mode}){const [name,setName]=useState(''); const [email,setEmail]=useState(mode==='login'?'user@fittrack.dev':''); const [password,setPassword]=useState(mode==='login'?'User@123':''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); const auth=useAuth(); const nav=useNavigate(); async function submit(e){e.preventDefault();setError('');setLoading(true);try{mode==='login'?await auth.login(email,password):await auth.register(name,email,password);nav('/dashboard')}catch(err){setError(err?.response?.data?.message||'Authentication failed')}finally{setLoading(false)}} return <div className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#14b8a633,transparent_35%),#07111f] p-6"><form onSubmit={submit} className="card w-full max-w-md p-8"><div className="mb-8"><Logo/></div><h1 className="text-3xl font-black">{mode==='login'?'Welcome back':'Create your account'}</h1><p className="mt-2 text-slate-400">{mode==='login'?'Use demo user credentials or your own account.':'Start tracking workouts and recovery today.'}</p>{error&&<div className="mt-5 rounded-xl border border-red-400/20 bg-red-500/10 p-3 text-sm text-red-200">{error}</div>}<div className="mt-6 space-y-4">{mode==='register'&&<input className="w-full" placeholder="Full name" value={name} onChange={e=>setName(e.target.value)}/>}<input className="w-full" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="w-full" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button disabled={loading} className="btn-primary w-full py-3">{loading?'Please wait...':mode==='login'?'Login':'Register'}</button></div><p className="mt-6 text-center text-sm text-slate-400">{mode==='login'?<>No account? <Link to="/register" className="text-teal-300">Register</Link></>:<>Already have account? <Link to="/login" className="text-teal-300">Login</Link></>}</p></form></div>}
